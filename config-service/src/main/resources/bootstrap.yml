spring:
  application:
    name: config-server
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/clkebr/ticketing-project-microservices-config
          default-label: master
          username: clkebr
          password: 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
      fail-fast: true

encrypt:
  key: cydeo

server:
  port: 8888
